using UnityEngine;
using System.Collections;

//! Class that defines a Reaction.
public class ReactionClass
{
    public string name;     /*!< String with Reaction name. */

    public int aMultipler;  /*!< Integer with 'A' multiplier. */
    public int bMultipler;  /*!< Integer with 'B' multiplier. */
    public int cMultipler;  /*!< Integer with 'C' multiplier. */
    public int dMultipler;  /*!< Integer with 'D' multiplier. */

    public string aName;    /*!< String with 'A' name. */
    public string bName;    /*!< String with 'B' name. */
    public string cName;    /*!< String with 'C' name. */
    public string dName;    /*!< String with 'D' name. */
}
